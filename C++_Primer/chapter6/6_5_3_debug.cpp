//#define NDEBUG
#include <iostream>
using namespace std;
#include <cassert>

void printErr(const string s, size_t size)
{
  // #ifndef NDEBUG
  // if (s.size()>size) {
  //   std::cerr << "error" << '\n';
  // }
  // #endif
  // assert (s.size()<size);
}

int main(int argc, char const *argv[]) {
  // string name ="boyao";
  // printErr(name,2);

  return 0;
}
